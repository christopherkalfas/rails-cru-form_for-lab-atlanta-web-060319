<%= form_for(@artist) do |f| %>
  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
    <br>
    <%= f.label :bio %>
    <%= f.text_field :bio %>
    <br>
  </div>

  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>